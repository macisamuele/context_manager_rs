name: Build and test

permissions:
  contents: read

on:
  workflow_dispatch:
  pull_request:
    branches: [ "main" ]
  push:
    branches: [ "main" ]
  schedule:
    # Run weekly (Monday 06:00 UTC) to catch breakage from dependency or toolchain drift
    - cron: '0 6 * * 1'

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: full

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.event.ref || github.sha }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        rust: [ stable, nightly ]

    steps:
    - uses: actions/checkout@v5
    - name: Install Rust (${{ matrix.rust }})
      uses: dtolnay/rust-toolchain@master
      with:
        toolchain: ${{ matrix.rust }}
        components: rustfmt, clippy
    - name: Format
      run: cargo fmt --check
    - name: Clippy (pedantic)
      # This might be very aggressive and possibly with false positive, but that's the point
      run: cargo clippy -- -D clippy::pedantic
    - name: Build
      run: cargo build --all-targets --verbose
    - name: Build doc
      run: cargo doc --all-features
    - name: Run tests
      run: cargo test --all-targets --verbose
